Drop default wallpaper images here (e.g. default-login.jpg,
default-standby.jpg, default-shutdown.jpg) and ship them in the package
if you want built-in choices instead of requiring users to point at a
path on disk.
